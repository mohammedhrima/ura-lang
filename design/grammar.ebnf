(* Ura — formal grammar (EBNF) *)
(* Status: PLANNED. To be written under improvement ④. *)
(*                                                            *)
(* Will cover: lexical (tokens, comments, indentation),       *)
(* syntactic (program → top_level → expr ladder),             *)
(* and the full operator precedence chain mirroring the       *)
(* AST_NODE macro chain in src/parse.c.                       *)
(*                                                            *)
(* See design/IMPROVEMENTS.md ④ for the work plan.            *)
