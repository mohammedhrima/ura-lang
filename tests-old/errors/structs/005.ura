// errors/structs/005 — instance method called via `::` instead of `.`
use "@/header"

struct Counter:
    n int

    fn bump() void:
        self.n += 1

main():
    c Counter
    c.n = 0
    Counter::bump()
