+ we have _build_load2 all over the code base, i did create load_if_necessary, and _load2, to fix this issue from the begiining, but here we are, is there a way we can do some standart solution, minimizing the number of functions (it will be nice if we can create 1 function that do everything for us), and it should handle everything, becasue i had to chnage a lot of stuff in append_output_arg, it's too confusing having the 3 of them, but we should also make sure to not break the codebase, if they generates a working code, then we update all test files to match the new output

+ add enums
+ implement templates
+ implement operator eveloading, to beused for exmaple in matrxes math operations, so we can implment string struct that work similar to modern languages works
+ switch/case ()
+ try/catch they should be allowed to throw a cosutm struct we well be building named
+ fileput, input, file read or something

+ add extension code in ura-lang code