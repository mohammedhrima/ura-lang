// functions/params/ref/002.ura - swap two weapon damage values via refs

fn swap_weapons(a int ref, b int ref) void:
    temp int = a
    a = b
    b = temp

main():
    sword  int = 30
    dagger int = 15
    swap_weapons(sword, dagger)
    output("<", sword, " ", dagger, ">\n")
