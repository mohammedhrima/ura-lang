// functions/basic/012.ura - everything: fn value + ref + void + arithmetic

fn twice(n int) int:
    return n + n
fn apply(f fn(int) int, x int) int:
    return f(x)
fn bump(ref n int) void:
    n = n + 1
main():
    n int = 20
    bump(n)
    return apply(twice, n)
