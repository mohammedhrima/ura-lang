func int to_ascii(char c):
    int n = c
    if n < 0 or n > 127: return -1
    // Check if it's a valid character
    if (n >= 97 and n <= 122) or  // a-z
       (n >= 65 and n <= 90) or   // A-Z
       (n >= 48 and n <= 57) or   // 0-9
       n == 32 or n == 0:         // space or null terminator
        return n
    return -1

func int strcmp(chars left, chars right):
    int i = 0
    while left[i] == right[i] and left[i] != '\0':
        i += 1
    return to_ascii(left[i]) - to_ascii(right[i])

main():
    int len = strcmp("b", "a")
    return len