// fn/param/ref/002.ura

fn swap(a int ref, b int ref) void:
    tmp int = a
    a = b
    b = tmp

main():
    x int = 10
    y int = 20
    swap(x, y)
    output("<", x, "> <", y, ">\n")  // <20> <10>
