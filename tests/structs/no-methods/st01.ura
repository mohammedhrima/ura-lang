// structs/no-methods/st01.ura
use "@/header"

struct User:
    age int
    name chars

main():
    user User
    user.age = 28
    name chars = calloc(1, 10)
    strcpy(name, "mohammed")
    printf("hello %s has %d\n", name, user.age)
    free(name)
