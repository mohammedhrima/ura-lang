// structs/methods/st02.ura
use "@/header"

struct User:
    name chars
    age int

    fn greed() void:
        printf("hello from %s\n", self.name)

main():
    user User
    user.name = "mohammed"
    user.greed()
