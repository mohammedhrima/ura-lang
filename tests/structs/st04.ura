proto fn printf(fmt chars, ...) int

struct User:
    name array[char]
    age int

    fn greed() void:
        printf("hello from %s\n", self.name)

main():
    user User
    user.name = "mohammed"
    user.greed()
