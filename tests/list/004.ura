// tests/list/004.ura
use "@/memory"

main():
    v List[int]
    v.push(5)
    v.push(10)
    v.push(15)
    v.push(20)

    total int = 0
    i int = 0
    while i < v.len():
        total += v[i]
        i += 1
    output("total=", total, "\n")
