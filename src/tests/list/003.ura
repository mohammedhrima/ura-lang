// tests/list/003.ura
use "@/memory"

main():
    v List[int]
    v.push(1)
    v.push(2)
    v.push(3)

    x int = v.pop()
    output("popped=", x, " len=", v.len(), "\n")

    y int = v.pop()
    output("popped=", y, " len=", v.len(), "\n")
