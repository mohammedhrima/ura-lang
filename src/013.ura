// 013: everything together — fn value + ref + void + arithmetic
// expect: 42
fn twice(n int) int:
    return n + n
fn apply(f fn(int) int, x int) int:
    return f(x)
fn bump(ref n int) void:
    n = n + 1
main():
    n int = 20
    bump(n)
    return apply(twice, n)